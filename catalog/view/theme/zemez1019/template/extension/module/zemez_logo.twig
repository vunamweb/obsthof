<h1 class="logo pull-left">
	{% if (logo) %} 
	<a href="./">
		<img src="{{ logo }}" title="{{ name }}" alt="{{ name }}" class="img-responsive">
	</a>
	{% else %} 
	<a href="{{ home }}">
		{{ name }} 
	</a>
	{% endif %} 
</h1>